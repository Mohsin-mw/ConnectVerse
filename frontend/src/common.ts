// Servers Type
export interface Servers {
  name: string;
  icon: string;
}

// Direct Messages Type

export interface DirectMessages {
  userName: string;
  avatar: string;
}

// User Type

export interface User {
  name: string;
  userName: string;
  avatar: string;
}
